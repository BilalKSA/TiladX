// Shared by the terms and privacy pages, so the two never drift apart.

// TODO: confirm the address before launch — the only mailbox referenced
// anywhere else in the repo is the no-reply sender in email.js.
export const CONTACT_EMAIL = 'info@tilad.org'

/** Shown at the top of both legal pages. Bump it whenever either text changes
 *  in a way a reader would care about. */
export const LEGAL_UPDATED = '٣ سبتمبر ٢٠٢٦'
