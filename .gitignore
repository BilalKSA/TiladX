# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-site
dist-app
dist-ssr
*.local
.firebase
email-previews
.env
.env.*
!.env.example
# Public origin URLs only, no secrets — needed by every production build.
!.env.production

.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Contains a plaintext admin password — never commit
supabase/admin-setup.sql

# Full-resolution source photos. Optimised copies live in public/assets/Gallery.
photo-originals/
